### Benchmarking the application locally

1. `bundle exec puma`
2. In a sperate window run `time benchmark/benchmark.sh`

### Running microbenchmarks

1. `bundle exec ruby benchmark/microbenchmarks.rb`
